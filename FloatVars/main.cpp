#include<iostream>
#include<conio.h> // I/O management

using namespace std;

int main() {
    float f = 2.567;
    float f2 = 1.3;
    cout << f + f2 << endl;
    getch(); // Reads a char from the console without using the buffer and without showing it on the output
    return 0;
}